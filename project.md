# 🧠 Memory Chatbot: Project Overview

## 📋 О проекте
**Memory Chatbot** — это автономный веб-клиент для LLM с расширенной долгосрочной памятью. Проект полностью работает в браузере (Client-side), используя API OpenRouter. 

Ключевая особенность — **проактивное формирование психологического портрета пользователя**. Бот не просто хранит историю переписки, а анализирует её, выделяет факты, черты характера, хронологию жизни и строит неочевидные гипотезы о пользователе.

## 🏗 Архитектура
Проект написан на **Vanilla JS** (ES6+) без систем сборки и фреймворков.

### Файловая структура
1.  **`index.html`** — Верстка, модальные окна, стили (CSS встроен или подключен).
2.  **`app.js`** — **Data Layer & UI Rendering**.
    *   Конфигурация (`CONFIG`).
    *   Управление `localStorage`.
    *   Функции отображения данных (рендеринг истории, вкладок памяти).
    *   Локализация и переводы.
3.  **`ui.js`** — **Logic Layer & AI Orchestration**.
    *   Обработка сообщений пользователя.
    *   Вызовы API (OpenRouter).
    *   Логика Tool Use (функции `get_user_facts` и т.д.).
    *   Фоновые процессы анализа (Facts/Traits/Timeline).
    *   Сложный цикл обновления гипотез (Hypotheses Lifecycle).

## 💾 Структура памяти (Knowledge Base)
Данные хранятся в `localStorage` в виде JSON-строк.

### 1. Основные категории (Plain Text)
*   **Facts:** Имена, локации, профессия.
*   **Traits:** Психологические черты, стиль общения.
*   **Timeline:** Важные жизненные события и даты.
*   **Style:** Инструкция для LLM по стилю общения с конкретным юзером.

### 2. Гипотезы (Structured Data)
Самая сложная часть памяти. Хранится как массив объектов:
```json
{
  "text": "Текст гипотезы с инсайтом",
  "confidence": 0.85,          // Уверенность (0.0 - 1.0)
  "evidence": ["факт 1", "факт 2"], // Массив доказательств
  "category": "psychology",    // Категория (motivation, fear, values и т.д.)
  "createdAt": 150,            // Номер сообщения при создании
  "updatedAt": 166,            // Номер сообщения при обновлении
  "revision": 2                // Счетчик изменений
}